<div class="comment row">
	<div class="comment-img col-sm-2 col-md-2 col-lg-2">
		<%= image_tag(comment.user.profile_image.url(:thumb), class: 'img-circle') %>
	</div>
	<div class="col-sm-10 col-md-10 col-lg-10">
		<div class="comment-title"><p><%= comment.user.username %></p></div>
	 	<%= comment.content %><br />
	 	<div class="comment-footer">
	 		<div class="col-sm-3 col-md-3 col-lg-3"><%= time_ago_in_words(comment.created_at)%></div>
		 	<%= link_to 'Edit', comment.to_edit_path %> |
		 	<%= link_to 'Delete', comment.to_path, method: :delete, data: { confirm: 'Are you sure?' } %>
	 	</div>
 	</div>
</div>